<UserControl x:Class="DeltaForceTracker.Controls.AchievementToast"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="340" Height="90"
             Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <QuadraticEase x:Key="EaseOut" EasingMode="EaseOut"/>

        <!-- Slide-in animation -->
        <Storyboard x:Key="SlideInStoryboard">
            <DoubleAnimation Storyboard.TargetName="ToastBorder"
                           Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                           From="400" To="0"
                           Duration="0:0:0.4"
                           EasingFunction="{StaticResource EaseOut}"/>
            <DoubleAnimation Storyboard.TargetName="ToastBorder"
                           Storyboard.TargetProperty="Opacity"
                           From="0" To="1"
                           Duration="0:0:0.3"/>
        </Storyboard>

        <!-- Fade-out animation -->
        <Storyboard x:Key="FadeOutStoryboard" Completed="FadeOutStoryboard_Completed">
            <DoubleAnimation Storyboard.TargetName="ToastBorder"
                           Storyboard.TargetProperty="Opacity"
                           From="1" To="0"
                           Duration="0:0:0.4"/>
        </Storyboard>
    </UserControl.Resources>

    <Border x:Name="ToastBorder"
            Background="{StaticResource CardBrush}"
            BorderBrush="{StaticResource AccentBrush}"
            BorderThickness="2"
            CornerRadius="8"
            Padding="15"
            RenderTransformOrigin="0.5,0.5"
            Effect="{StaticResource CardShadow}"
            Opacity="0">
        <Border.RenderTransform>
            <TranslateTransform X="400"/>
        </Border.RenderTransform>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Achievement Icon -->
            <Image x:Name="AchIcon"
                   Grid.Column="0"
                   Width="50" Height="50"
                   Margin="0,0,12,0"
                   VerticalAlignment="Center"/>

            <!-- Text Content -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="Achievement Unlocked!"
                          FontFamily="{StaticResource MainFont}"
                          FontSize="11"
                          FontWeight="SemiBold"
                          Foreground="{StaticResource AccentBrush}"
                          Margin="0,0,0,5"/>
                
                <TextBlock x:Name="AchTitle"
                          Text="Achievement Title"
                          FontFamily="{StaticResource MainFont}"
                          FontSize="16"
                          FontWeight="Bold"
                          Foreground="{StaticResource TextPrimaryBrush}"
                          TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
