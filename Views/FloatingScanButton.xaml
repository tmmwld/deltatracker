<Window x:Class="DeltaForceTracker.Views.FloatingScanButton"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Scan"
        Width="50" 
        Height="50"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    
    <Border Background="#1A1F2E" 
            BorderBrush="#00FF88" 
            BorderThickness="2" 
            CornerRadius="25"
            Opacity="0.85"
            MouseEnter="Border_MouseEnter"
            MouseLeave="Border_MouseLeave">
        
        <Button Content="💰"
                FontSize="24"
                Background="Transparent"
                BorderThickness="0"
                Foreground="#00FF88"
                Cursor="Hand"
                Click="ScanButton_Click"
                ToolTip="Triple-click OR click here to scan"
                Padding="0">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
    </Border>
</Window>
