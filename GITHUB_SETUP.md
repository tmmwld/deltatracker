# Как залить проект на GitHub - Пошаговая инструкция

## 📍 Где сейчас находится код?

Код находится **локально на вашем Mac** в папке:
```
/Users/vadim/.gemini/antigravity/scratch/DeltaForceTracker
```

Я **НЕ** могу сам запушить код на GitHub, так как:
- У меня нет доступа к вашему аккаунту GitHub
- Я не могу выполнять git push от вашего имени
- Вам нужно сделать это самостоятельно

## 🚀 Пошаговая инструкция

### Шаг 1: Создайте репозиторий на GitHub

1. **Откройте браузер** и зайдите на https://github.com/
2. **Войдите** в свой аккаунт (если еще не вошли)
3. **Нажмите** на зеленую кнопку **"New"** или **"+"** в правом верхнем углу
4. **Выберите** "New repository"

![Создание репозитория](https://docs.github.com/assets/cb-11427/mw-1440/images/help/repository/repo-create.webp)

5. **Заполните форму**:
   - **Repository name**: `DeltaForceTracker` (или любое другое имя)
   - **Description**: "OCR balance tracker for Delta Force game"
   - **Public** или **Private**: выберите Public (бесплатный GitHub Actions)
   - ⚠️ **НЕ ставьте галочки** на "Add a README file", "Add .gitignore", "Choose a license"
     (у нас уже есть эти файлы)

6. **Нажмите** зеленую кнопку **"Create repository"**

### Шаг 2: Скопируйте URL репозитория

После создания репозитория GitHub покажет вам страницу с инструкциями.

**Найдите** строку типа:
```
https://github.com/ВАШ_USERNAME/DeltaForceTracker.git
```

**Скопируйте** этот URL (он понадобится в следующем шаге).

### Шаг 3: Откройте терминал на Mac

1. Нажмите **Cmd + Пробел**
2. Напишите **"Terminal"**
3. Нажмите **Enter**

### Шаг 4: Перейдите в папку проекта

В терминале выполните:

```bash
cd /Users/vadim/.gemini/antigravity/scratch/DeltaForceTracker
```

### Шаг 5: Инициализируйте Git репозиторий

```bash
git init
```

**Вы увидите**: `Initialized empty Git repository...`

### Шаг 6: Добавьте все файлы

```bash
git add .
```

Эта команда добавит все файлы проекта в git.

### Шаг 7: Сделайте первый коммит

```bash
git commit -m "Initial commit: Delta Force Balance Tracker v1.0"
```

**Вы увидите**: список добавленных файлов.

### Шаг 8: Укажите ветку main

```bash
git branch -M main
```

### Шаг 9: Добавьте ссылку на GitHub репозиторий

⚠️ **ЗАМЕНИТЕ** `ВАШ_USERNAME` на ваш реальный username на GitHub!

```bash
git remote add origin https://github.com/ВАШ_USERNAME/DeltaForceTracker.git
```

**Пример** (если ваш username `ivan123`):
```bash
git remote add origin https://github.com/ivan123/DeltaForceTracker.git
```

### Шаг 10: Запушьте код на GitHub

```bash
git push -u origin main
```

**Возможно** система попросит ввести:
- Username на GitHub
- Password (используйте **Personal Access Token**, не пароль!)

#### Если просит пароль - создайте Personal Access Token:

1. Зайдите на https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Выберите срок действия: **No expiration**
4. Поставьте галочку **"repo"** (все подгалочки под ним)
5. Нажмите **"Generate token"**
6. **Скопируйте** токен (он больше не покажется!)
7. **Используйте этот токен** вместо пароля в терминале

### Шаг 11: Проверьте на GitHub

1. Обновите страницу вашего репозитория на GitHub
2. Вы должны увидеть все файлы проекта
3. В том числе папку `.github/workflows/` с файлом `build.yml`

### Шаг 12: Дождитесь сборки

1. Перейдите на вкладку **"Actions"** в вашем репозитории
2. Вы увидите workflow **"Build Windows Executable"** в процессе выполнения
3. Подождите **5-10 минут** пока соберется
4. Когда появится зеленая галочка ✅ - сборка готова

### Шаг 13: Скачайте собранное приложение

1. Нажмите на название workflow (Build Windows Executable)
2. Прокрутите вниз до раздела **"Artifacts"**
3. Нажмите на **"DeltaForceTracker-Windows"** чтобы скачать ZIP
4. Скачается архив с вашим приложением

## 📋 Полная последовательность команд (копи-паста)

Откройте терминал и выполните **по очереди**:

```bash
# 1. Перейти в папку проекта
cd /Users/vadim/.gemini/antigravity/scratch/DeltaForceTracker

# 2. Инициализировать git
git init

# 3. Добавить все файлы
git add .

# 4. Сделать коммит
git commit -m "Initial commit: Delta Force Balance Tracker v1.0"

# 5. Установить ветку main
git branch -M main

# 6. Добавить remote (ЗАМЕНИТЕ ВАШ_USERNAME!)
git remote add origin https://github.com/ВАШ_USERNAME/DeltaForceTracker.git

# 7. Запушить
git push -u origin main
```

⚠️ **НЕ ЗАБУДЬТЕ** заменить `ВАШ_USERNAME` в команде №6!

## ❓ Возможные проблемы

### "remote origin already exists"

**Решение:**
```bash
git remote remove origin
git remote add origin https://github.com/ВАШ_USERNAME/DeltaForceTracker.git
git push -u origin main
```

### "Permission denied"

**Причина**: Нужен Personal Access Token

**Решение**: См. "Шаг 10" выше про создание токена

### "Updates were rejected"

**Решение:**
```bash
git pull origin main --rebase
git push -u origin main
```

### Как узнать свой GitHub username?

1. Зайдите на github.com
2. Нажмите на ваш аватар в правом верхнем углу
3. Username написан там

**Или** откройте https://github.com/ - username будет в URL (github.com/username)

## 🎯 После успешного push

Вы увидите на GitHub:
- ✅ Все файлы проекта
- ✅ В Actions запустится сборка
- ✅ Через 10 минут будет готовый .exe в артефактах

## 📞 Если что-то не получается

**Напишите мне:**
1. Какую команду выполняли
2. Какую ошибку получили (скопируйте весь текст)
3. Я помогу решить проблему

---

**Давайте, пушьте! Жду когда увидите свой репозиторий на GitHub 😊**
